export enum EventStatus {
  READY = 'ready',
  STARTED = 'started',
  COMPLETED = 'completed'
}

export enum EventQuality {
  REGULAR = 'regular',
  RARE = 'rare',
  EPIC = 'epic',
  LEGENDARY = 'legendary'
}

export enum EventsVotingStatus {
  STARTED = 'started',
  COMPLETED = 'completed'
}
